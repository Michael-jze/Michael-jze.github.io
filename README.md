# Michael-jze.github.io



- Project init:

  ```bash
  quarto create project website Michael-jze.github.io
  quarto preview
  ```

- Project publish:

  ```bash
  quarto publish gh-pages
  git add . && git commit -m "mod" && git push
  ```

  